Install a debian 10 without Desktop, so you have to select only
System tools from the list. I also selected SSH server for
convenience, but that is not necessary if you do not want to
login remotely.
